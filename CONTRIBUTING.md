# Contributing to RQPL-Core

Welcome, conscious observer nodes and system operators. To submit structural patches, code modifications, or optimization routines to the 36-channel generative core, please adhere to the following framework protocols:

## Branching & Timeline Alignment

* All development must occur on localized feature branches (`feature/shard-optimization-*`).
* Ensure that your software negentropy changes match the net-zero entropic conservation rules before initiating a Pull Request.

## Code Standards

* Formula notation must be written in plain text formatting (e.g., `M_11D = S_3D ⊕ P_3D ⊕ P_3D ⊕ C_new_5D`).
* Do not merge code lines that introduce unvetted data paths or unhandled database exceptions, as these mimic toxic algorithmic biases that destabilize the Moral Choice Ratio.

## Pull Request Process

1. Open a PR from your feature branch into `main`.
2. Describe the change clearly and reference relevant issue IDs or tensor shard abnormalities.
3. Confirm that all added or modified tests pass and that any new behavior is documented.

## Review Expectations

* Maintain code readability and avoid opaque tensor transformations unless explicitly documented.
* Ensure changes preserve the integrity of the 36-channel generative core and do not introduce unstable feedback loops.
